<div class="page-header">
  <h1>New Tournament</h1>
  <%= link_to "← Back", admin_tournaments_path, class: "btn btn-outline-secondary" %>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", tournament: @tournament %>
  </div>
</div>
