<div class="page-header">
  <h1>Edit: <%= @tournament.name %></h1>
  <%= link_to "← Back", admin_tournament_path(@tournament), class: "btn btn-outline-secondary" %>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", tournament: @tournament %>
  </div>
</div>
